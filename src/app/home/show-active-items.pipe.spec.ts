import { ShowActiveItemsPipe } from './show-active-items.pipe';

describe('ShowActiveItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowActiveItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
